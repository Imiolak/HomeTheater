<UserControl x:Class="HomeTheater.GUI.Panels.GUIInputPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>    
        
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Margin="5" Click="PreviousTrackButton_Click">Previous</Button>
            <Button Margin="5" Click="StopButon_Click">Stop</Button>
            <Button Margin="5" Click="PlayPauseButton_Click">Play/Pause</Button>
            <Button Margin="5" Click="NextTrackButton_Click">Next</Button>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Margin="5" Click="VolumeDownButtonClick">Volume Down</Button>
            <Button Margin="5" Click="MuteButton_Click">Mute</Button>
            <Button Margin="5" Click="VolumeUpButton_Click">Volume Up</Button>
        </StackPanel>
        
    </Grid>
        
</UserControl>
